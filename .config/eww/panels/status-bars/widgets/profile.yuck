(defwidget
 bar-profile []
 (eventbox :class "widget profile"
           :onclick "eww open --toggle powermenu-panel"
           :cursor "pointer"

           (box :class "face"
                :width status-bar-size
                :height status-bar-size
                :style "background-image: url('${profile-face}');")))
