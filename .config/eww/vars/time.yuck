(defpoll time
         :initial "0:00 AM"
         :interval "1s"
         "date '+%-l:%M %p'")

(defpoll date
         :initial "1 jan 1970"
         :interval "1h"
         "date '+%a, %-d %b %Y'")

(defpoll year
         :initial 1970
         :interval "24h"
         "date +%Y")
